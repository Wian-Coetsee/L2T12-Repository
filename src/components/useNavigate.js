import { useNavigate as useReactRouterNavigate } from 'react-router-dom';

// Custom hook to use the navigate function from react-router-dom
const useNavigate = () => useReactRouterNavigate();

export default useNavigate;
